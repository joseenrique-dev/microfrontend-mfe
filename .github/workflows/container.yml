name: deploy-container

on:
  push:
    branches:
      - master
    paths:
      - "packages/container/**"

defaults:
  run:
    working-directory: packages/container

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v2
      - run: npm install
      - run: npm run build

      - uses: shinyinc/action-aws-cli@v1.2
      - run: aws s3 sync dist s3://${{ secrets.AWS_S3_BUCKET_NAME }}/container/latest
        env:
          AWS_ACCESS_KEY_ID: "AKIAXIXSKNKX5J3M7X4O"
          AWS_SECRET_ACCESS_KEY: "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"
          AWS_DEFAULT_REGION: ""